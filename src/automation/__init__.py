"""Ambient automation: scheduled, unattended tasks that run while Argent is up.

Distinct from the interactive agent loop — nobody is watching, so the rules are
different: a bounded toolset, a turn budget, and every gated action refused
rather than auto-approved.
"""
